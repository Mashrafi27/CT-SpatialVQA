# Deprecated: Old Folder Layout Notes

This file is kept only for historical context. The repository has been reorganized and no longer uses the `3D_VLM_Spatial/` top‑level layout.

Use these entry points instead:

- Main paper + dataset overview: repository root
- QA generation pipeline details: `QA_generation/`
- Benchmarking (preprocess/inference/eval) details: `benchmarking/`
